name: Continuous Integration

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
            
      - name: YAML lint on Ansible
        continue-on-error: true
        run: yamllint -c ./tests/ymllint/config.yml ./ansible 
        
      - name: PSScriptAnalyzer on Ansible
        continue-on-error: true
        run: pwsh -Command Invoke-ScriptAnalyzer -Path "./ansible/*" -Setting "./tests/psscript/config.psd1" -EnableExit

      - name: PSScriptAnalyzer on Nutanix automation
        continue-on-error: true
        run: pwsh -Command Invoke-ScriptAnalyzer -Path "./scripts/nutanix","./init/nutanix" -Setting "./tests/psscript/config.psd1" -EnableExit

      - name: Install Packer
        uses: hashicorp/setup-packer@main

      - name: Validate Nutanix Packer templates
        continue-on-error: true
        run: |
          for template in server/2019 server/2022 server/2025 desktop/11; do
            packer init "./packer/nutanix/windows/$template"
            packer validate -syntax-only "./packer/nutanix/windows/$template"
          done
      
      - name: TFLint for Terraform
        continue-on-error: true
        run: |
          curl -s https://raw.githubusercontent.com/terraform-linters/tflint/master/install_linux.sh | bash
          tflint --format junit --recursive > tflint-result.xml
      
      - name: Checkov for Terraform
        continue-on-error: true
        run: |
          pip3 install checkov
          checkov --directory ./terraform -o junitxml > checkov-result.xml

      - name: Publish test results
        uses: EnricoMi/publish-unit-test-result-action@v2
        if: always()
        with:
          files: |
            *-result.xml
