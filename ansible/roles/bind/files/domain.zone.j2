;$TILL 2d

$ORIGIN {{domain_name}}.

@               IN      SOA     ns.{{domain_name}}. info.{{domain_name}}. (
                                2022122800      ; serial
                                12h             ; refresh
                                15m             ; retry
                                3w              ; expire
                                2h              ; minimum ttl
                                )

                IN      NS      ns.{{domain_name}}.

ns              IN      A       {{ansible_default_ipv4.address}}
; ----------------------------------------
